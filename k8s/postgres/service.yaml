apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: parish
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP so postgres is only reachable inside the cluster
  type: ClusterIP
